<UserControl x:Class="QM.Manager.Views.MetadataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QM.Manager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid x:Name="Datas" 
                  IsReadOnly="True" 
                  AutoGenerateColumns="False" 
                  FrozenColumnCount="1"
                  SelectionUnit="Cell">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Key}" Header="" />
                <DataGridTextColumn Binding="{Binding Value}" Header="" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
