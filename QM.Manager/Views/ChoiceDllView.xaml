<UserControl x:Class="QM.Manager.Views.ChoiceDllView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QM.Manager.Views"
             xmlns:cm="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             xmlns:ac="clr-namespace:AsNum.Common.WPF;assembly=AsNum.Common.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <ListView x:Name="Datas" SelectedValue="{Binding Current}">
        </ListView>

        <StackPanel ac:UniPadding.Padding="5" Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button x:Name="Update" Content="刷新" Width="100" />
            <Button x:Name="Choice" Content="选择" Width="100" />
        </StackPanel>
    </Grid>
</UserControl>
