<UserControl x:Class="QM.Shell.Views.ListTriggerListenerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0" x:Name="ScheduleListeners" IsEnabled="False">
            
        </DataGrid>
        <DataGrid Grid.Row="1" x:Name="TriggerListeners" IsEnabled="False">

        </DataGrid>
        <DataGrid Grid.Row="2" x:Name="JobListeners" IsEnabled="False">

        </DataGrid>
    </Grid>
</UserControl>
